/* Test phases
 * - Create an active contract
 * - Create an order
 * - Modify an order
 * - Create a partially matching order on opposite side (should be partially matched)
 * - Modify the original order again
 * - Create a partially matching order on opposite side (should be partially matched)
 * - Cancel the original order
 * - Verify none of the orders is active and there are two trades
*/